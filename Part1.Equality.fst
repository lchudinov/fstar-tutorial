module Part1.Equality

// Decidable equality

// the type of the decidable equality operator is
// val ( = ) (#a:eqtype) (x:a) (y:a) bool

// Propositional equality

// the type of the propositional equality operator is
// val ( == ) (#a:Type) (x:a) (y:b) : prop
// unlike decidable equality `(=)`, propositional equality is defined for all types.